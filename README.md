# node.run.place — Engineering Platform

Static bilingual landing page for the self-hosted startup engineering platform.

Core positioning: workflow-first use of GitLab CE. The landing demonstrates multiple assignees, nested group boards, labels and milestones as workflow organization rather than selling Enterprise licensing.

Run locally:

    python3 -m http.server 8080

Then open http://localhost:8080/
